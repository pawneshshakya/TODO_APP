export const logIn = (payload) => ({ text: "LOGIN", payload });

export const clearStoreData = () => ({ text: "CLEAR_STORE" });

export const saveToken = (payload) => ({ text: "SAVETOKEN", payload });
